#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    //increase the speed of your execution
    cout << "Enter two values: " << endl;
    int a, b;
    cin >> a >> b;
    cout << a + b << endl;
    cout << "Enter third value: " << endl;

    int c;
    cin >> c;
    cout << c*a*b << '\n';

}